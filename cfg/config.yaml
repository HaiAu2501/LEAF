defaults:
  - alg: rf_cls
  - _self_

dataset:
  _target_: utils.loader.Dataset
  model: gpt-4o-mini # Can be null if not using LLM-based annotations
  name: credit-g
  ratio: [0.4, 0.4, 0.2] # train, val, test

evaluator:
  _target_: src.evaluator.Evaluator
  model: gpt-4o-mini # Can be null if not using LLM priors
  n_data_splits: 5
  n_alg_runs: 3
  random_state: 0
  verbose: True

alg:
  param_grid:
    n_estimators: [50] # fixed for fair comparison
    max_depth: [3] # fixed for fair comparison

paths:
  project_root: .
  log_dir: ./logs
